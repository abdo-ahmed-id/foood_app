import 'package:flutter/material.dart';

class MainFoodPage extends StatelessWidget {
  bool appBar = true;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Icon(Icons.cloud_off_outlined)),
    );
  }
}
